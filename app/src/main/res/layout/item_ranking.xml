<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <ImageView
        android:id="@+id/iv_rank_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="#F5F5F5"
        android:padding="6dp"
        android:src="@drawable/ic_food"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_rank_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:text="餐饮"
        android:textColor="#333333"
        android:textSize="14sp"
        app:layout_constraintStart_toEndOf="@id/iv_rank_icon"
        app:layout_constraintTop_toTopOf="@id/iv_rank_icon" />

    <TextView
        android:id="@+id/tv_rank_percent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="45.5%"
        android:textColor="#999999"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="@id/tv_rank_name"
        app:layout_constraintStart_toEndOf="@id/tv_rank_name" />

    <TextView
        android:id="@+id/tv_rank_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="120.00"
        android:textColor="#333333"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_rank_name" />

    <ProgressBar
        android:id="@+id/pb_rank_bar"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="0dp"
        android:layout_height="6dp"
        android:layout_marginTop="8dp"
        android:progress="60"
        android:progressDrawable="@drawable/bg_ranking_progress"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@id/tv_rank_name"
        app:layout_constraintTop_toBottomOf="@id/tv_rank_name" />

</androidx.constraintlayout.widget.ConstraintLayout>